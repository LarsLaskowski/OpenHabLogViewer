The codebase uses TypeScript ES modules, explicit exported types/interfaces, small focused modules, and mostly functional DOM rendering on the client. Naming is descriptive camelCase/PascalCase, types are shared between client/server via mirrored interfaces, and styling is plain CSS with semantic class names. Keep changes surgical and aligned with existing direct DOM APIs and CSS grid layout.